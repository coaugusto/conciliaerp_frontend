# Módulos de domínio

Cada domínio do MVP (`auth`, `dashboard`, `connections`, `data-sources`, `rules`, `executions` e `alerts`) é isolado gradualmente aqui. As telas atuais compartilham componentes e serviços para manter a primeira fase enxuta; a camada de API já está separada em `src/services`.
