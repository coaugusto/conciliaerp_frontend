"use client";
import { useQuery } from "@tanstack/react-query";
import { Search, ShieldCheck } from "lucide-react";
import { Card, ErrorState, PageHeader } from "@/components/shared/ui";
import { masterCatalogService } from "@/services/master-catalog.service";
export default function Marketplace(){const query=useQuery({queryKey:["marketplace"],queryFn:masterCatalogService.list});if(query.isLoading)return <p className="p-8 text-sm text-slate-500">Carregando catálogo global...</p>;if(query.isError)return <ErrorState message="Não foi possível consultar o Marketplace."/>;return <><PageHeader title="Marketplace do Catálogo Mestre" description="Produtos globais aprovados, prontos para enriquecer seu cadastro privado."/><Card className="mb-5 flex items-center gap-3 p-4 text-sm text-slate-600"><ShieldCheck className="text-emerald-600"/> Somente itens publicados e submetidos ao fluxo de qualidade são exibidos.</Card><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{query.data?.map(product=><Card key={product.id} className="p-5"><div className="flex items-start justify-between gap-3"><h2 className="font-bold text-slate-900">{product.canonicalDescription}</h2><span className="rounded bg-emerald-50 px-2 py-1 text-xs font-semibold text-emerald-700">{Math.round(Number(product.confidence)*100)}%</span></div><p className="mt-3 text-sm text-slate-600">{product.brand??"Marca não informada"} · {product.manufacturer??"Fabricante não informado"}</p><dl className="mt-4 grid grid-cols-2 gap-3 text-xs"><div><dt className="text-slate-400">GTIN</dt><dd>{product.gtin??"—"}</dd></div><div><dt className="text-slate-400">NCM</dt><dd>{product.ncm??"—"}</dd></div><div><dt className="text-slate-400">Categoria</dt><dd>{product.category??"—"}</dd></div><div><dt className="text-slate-400">Versão</dt><dd>{product.version}</dd></div></dl></Card>)}{!query.data?.length&&<p className="text-sm text-slate-500">Nenhum produto publicado no catálogo global.</p>}</div></>}
