"use client";
import { useState } from "react"; import { useQuery } from "@tanstack/react-query"; import { Button, Card, ErrorState, PageHeader, StatusBadge } from "@/components/shared/ui"; import { connectorDataService, extractionTypes, type ExtractionType } from "@/services/connector-data.service";
const label=(type:string)=>extractionTypes.find(([id])=>id===type)?.[1]??type;
export default function ConnectorDataPage(){const[entityType,setEntityType]=useState<ExtractionType>("MASTER_PRODUCTS_V1"),[selected,setSelected]=useState<Record<string,unknown>|null>(null),summary=useQuery({queryKey:["connector-data-summary"],queryFn:connectorDataService.summary}),records=useQuery({queryKey:["connector-data",entityType],queryFn:()=>connectorDataService.list(entityType)});return <><PageHeader title="Dados extraídos do ERP" description="Dados recebidos pelo Connector, segregados por empresa e consulta."/>{summary.isError?<ErrorState message="Não foi possível carregar o resumo das extrações."/>:<div className="mb-6 grid gap-3 md:grid-cols-2 xl:grid-cols-4">{summary.data?.map(item=><Card key={item.entityType} className="p-4"><p className="text-xs text-slate-500">{label(item.entityType)}</p><p className="mt-1 text-2xl font-semibold text-slate-800">{item.total.toLocaleString("pt-BR")}</p><p className="mt-2 text-xs text-slate-500">{item.lastReceivedAt?`Recebido: ${new Date(item.lastReceivedAt).toLocaleString("pt-BR")}`:"Sem carga"}</p></Card>)}</div>}<Card className="p-4"><div className="mb-4 flex flex-wrap items-center gap-3"><select className="rounded border p-2 text-sm" value={entityType} onChange={event=>{setEntityType(event.target.value as ExtractionType);setSelected(null)}}>{extractionTypes.map(([id,name])=><option key={id} value={id}>{name}</option>)}</select><StatusBadge value={records.isFetching?"RUNNING":"COMPLETED"}/><span className="text-sm text-slate-500">{records.data?.total??0} registros</span></div>{records.isError?<ErrorState message="Não foi possível carregar os dados recebidos."/>:<table className="w-full text-left text-sm"><thead><tr className="border-b bg-slate-50 text-xs uppercase text-slate-500"><th className="p-3">Chave de origem</th><th className="p-3">Alteração no ERP</th><th className="p-3">Recebido em</th><th className="p-3"/></tr></thead><tbody>{records.data?.items.map(item=><tr className="border-b" key={item.id}><td className="p-3 font-mono text-xs">{item.sourceKey}</td><td className="p-3">{item.sourceChangedAt?new Date(item.sourceChangedAt).toLocaleString("pt-BR"):"—"}</td><td className="p-3">{new Date(item.receivedAt).toLocaleString("pt-BR")}</td><td className="p-3"><Button variant="ghost" onClick={()=>setSelected(item.payload)}>Detalhes</Button></td></tr>)}</tbody></table>}{!records.isLoading&&!records.data?.items.length&&<p className="py-10 text-center text-sm text-slate-500">Nenhum lote desta consulta foi recebido ainda.</p>}</Card>{selected&&<Card className="mt-5 p-4"><div className="mb-3 flex items-center justify-between"><b>Payload recebido</b><Button variant="secondary" onClick={()=>setSelected(null)}>Fechar</Button></div><pre className="max-h-[480px] overflow-auto rounded bg-slate-950 p-4 text-xs text-white">{JSON.stringify(selected,null,2)}</pre></Card>}</>}
