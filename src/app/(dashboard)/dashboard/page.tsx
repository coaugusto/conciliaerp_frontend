"use client";
import { useQuery } from "@tanstack/react-query";
import { AlertTriangle, CheckCircle2, FileSearch, ShieldAlert, TrendingUp } from "lucide-react";
import { Bar, BarChart, CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { Card, ErrorState, PageHeader, SeverityBadge, StatusBadge, money } from "@/components/shared/ui";
import { dashboardService } from "@/services/dashboard.service";
export default function Dashboard(){const {data,isLoading,isError}=useQuery({queryKey:["dashboard","summary"],queryFn:dashboardService.summary});if(isLoading)return <div className="space-y-5">{[1,2,3].map(i=><div key={i} className="h-32 animate-pulse rounded-xl bg-slate-200"/>)}</div>;if(isError||!data)return <ErrorState/>;const metrics=[{label:"Documentos analisados",value:data.totalAnalyzed.toLocaleString("pt-BR"),icon:FileSearch,color:"text-blue-600"},{label:"Documentos conciliados",value:data.totalMatched.toLocaleString("pt-BR"),icon:CheckCircle2,color:"text-emerald-600"},{label:"Documentos divergentes",value:data.totalDivergent.toLocaleString("pt-BR"),icon:AlertTriangle,color:"text-orange-600"},{label:"Percentual de conciliação",value:data.reconciliationRate.toLocaleString("pt-BR")+"%",icon:TrendingUp,color:"text-violet-600"},{label:"Valor total divergente",value:money(data.totalDifferenceAmount),icon:ShieldAlert,color:"text-red-600"},{label:"Alertas abertos",value:data.openAlerts.toString(),icon:ShieldAlert,color:"text-amber-600"}];return <><PageHeader title="Dashboard" description="Visão consolidada das conciliações e divergências." action={<button className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm">24/07/2026 — 24/07/2026</button>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{metrics.map(m=>{const Icon=m.icon;return <Card key={m.label} className="p-5"><div className="flex justify-between"><span className="text-sm text-slate-500">{m.label}</span><Icon size={19} className={m.color}/></div><p className="mt-3 text-2xl font-bold tracking-tight">{m.value}</p><p className="mt-1 text-xs text-slate-400">Período selecionado</p></Card>})}</div><div className="mt-5 grid gap-5 xl:grid-cols-2"><Card className="p-5"><h2 className="font-semibold">Evolução diária das divergências</h2><p className="mb-4 text-xs text-slate-500">Últimos 7 dias</p><div className="h-64"><ResponsiveContainer><LineChart data={data.dailyDivergences}><CartesianGrid strokeDasharray="3 3" stroke="#e2e8f0"/><XAxis dataKey="date" fontSize={12}/><YAxis fontSize={12}/><Tooltip/><Line type="monotone" dataKey="total" stroke="#2563eb" strokeWidth={3} dot={{r:4}}/></LineChart></ResponsiveContainer></div></Card><Card className="p-5"><h2 className="font-semibold">Alertas por severidade</h2><p className="mb-4 text-xs text-slate-500">Alertas identificados no período</p><div className="h-64"><ResponsiveContainer><BarChart data={data.alertsBySeverity}><CartesianGrid strokeDasharray="3 3" stroke="#e2e8f0"/><XAxis dataKey="severity" fontSize={12}/><YAxis fontSize={12}/><Tooltip/><Bar dataKey="total" fill="#f97316" radius={[4,4,0,0]}/></BarChart></ResponsiveContainer></div></Card></div><div className="mt-5 grid gap-5 xl:grid-cols-2"><Card><div className="border-b border-slate-100 p-5"><h2 className="font-semibold">Últimas execuções</h2></div>{data.latestExecutions.map(e=><div key={e.id} className="flex items-center justify-between border-b border-slate-100 p-4 last:border-0"><div><p className="text-sm font-semibold">{e.rule}</p><p className="text-xs text-slate-500">{e.id} · {e.analyzed.toLocaleString("pt-BR")} analisados</p></div><StatusBadge value={e.status}/></div>)}</Card><Card><div className="border-b border-slate-100 p-5"><h2 className="font-semibold">Alertas de maior valor</h2></div>{data.highestValueAlerts.slice(0,3).map(a=><div key={a.id} className="flex items-center justify-between border-b border-slate-100 p-4 last:border-0"><div><p className="flex gap-2 text-sm font-semibold"><SeverityBadge value={a.severity}/>{a.document}</p><p className="mt-1 text-xs text-slate-500">{a.person}</p></div><b className="text-sm text-red-700">{money(a.difference)}</b></div>)}</Card></div></>}
