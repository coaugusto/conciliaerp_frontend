"use client";
import { Check, KeyRound } from "lucide-react";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/shared/ui";
import { useAuth } from "@/providers/providers";
export default function ChangePassword(){const [current,setCurrent]=useState(""),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[error,setError]=useState(""),[saving,setSaving]=useState(false),router=useRouter(),{changePassword}=useAuth(); const valid=password.length>=8&&/[A-Z]/.test(password)&&/\d/.test(password);const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!current||!valid||password!==confirm)return setError("Revise os campos de senha.");setSaving(true);setError("");try{await changePassword(current,password);router.replace("/dashboard");}catch(error){setError(error instanceof Error?error.message:"Não foi possível alterar a senha.");}finally{setSaving(false);}};return <main className="grid min-h-screen place-items-center bg-slate-50 p-6"><form onSubmit={submit} className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-8 shadow-sm"><div className="grid size-11 place-items-center rounded-xl bg-blue-100 text-blue-700"><KeyRound size={22}/></div><h1 className="mt-5 text-2xl font-bold">Atualize sua senha</h1><p className="mt-2 text-sm leading-6 text-slate-500">Por segurança, é necessário definir uma nova senha antes de acessar o sistema.</p>{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}<label className="mt-5 block text-sm font-semibold">Senha atual<input type="password" value={current} onChange={e=>setCurrent(e.target.value)} className="mt-1.5 h-10 w-full rounded-lg border border-slate-300 px-3"/></label><label className="mt-4 block text-sm font-semibold">Nova senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-1.5 h-10 w-full rounded-lg border border-slate-300 px-3"/></label><label className="mt-4 block text-sm font-semibold">Confirmar nova senha<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} className="mt-1.5 h-10 w-full rounded-lg border border-slate-300 px-3"/></label><div className="mt-4 rounded-lg bg-slate-50 p-3 text-xs text-slate-600"><p className="mb-2 font-semibold">Requisitos da senha</p>{["Ao menos 8 caracteres","Uma letra maiúscula","Um número"].map((t,i)=><p key={t} className="flex gap-2 py-0.5"><Check size={14} className={(i===0?password.length>=8:i===1?/[A-Z]/.test(password):/\d/.test(password))?"text-emerald-600":"text-slate-300"}/>{t}</p>)}</div><Button disabled={saving} className="mt-6 h-11 w-full">{saving?"Salvando...":"Salvar nova senha"}</Button></form></main>}
