"use client";
import { Eye, EyeOff, LockKeyhole, ShieldCheck } from "lucide-react";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { useAuth } from "@/providers/providers";
import { Button } from "@/components/shared/ui";
export default function LoginPage(){const [email,setEmail]=useState("admin@conciliaerp.com.br"),[password,setPassword]=useState("admin123"),[show,setShow]=useState(false),[error,setError]=useState(""),[loading,setLoading]=useState(false),router=useRouter(),{login}=useAuth(); const submit=async(e:React.FormEvent)=>{e.preventDefault();setError("");setLoading(true);try{const u=await login(email,password);router.replace(u.mustChangePassword?"/change-password":"/dashboard")}catch(err){setError(err instanceof Error?err.message:"Não foi possível conectar à API.")}finally{setLoading(false)}};return <main className="grid min-h-screen bg-slate-50 lg:grid-cols-[1.1fr_.9fr]"><section className="hidden bg-slate-950 p-12 text-white lg:flex lg:flex-col"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-blue-600 text-lg font-bold">C</span><b>Concilia ERP</b></div><div className="my-auto max-w-lg"><p className="mb-4 text-sm font-semibold tracking-widest text-blue-400">AUDITORIA INTELIGENTE</p><h1 className="text-4xl font-bold leading-tight">Confiança em cada conciliação.</h1><p className="mt-5 text-lg leading-8 text-slate-400">Identifique títulos sem contabilização, trate diferenças e mantenha a integridade financeira do seu ERP.</p></div><div className="flex gap-3 text-sm text-slate-400"><ShieldCheck className="text-blue-400"/> Acesso seguro e rastreável</div></section><section className="flex items-center justify-center p-6"><form onSubmit={submit} className="w-full max-w-sm"><div className="mb-10 lg:hidden"><b className="text-xl">Concilia ERP</b></div><p className="text-sm font-semibold text-blue-700">BEM-VINDO</p><h2 className="mt-1 text-3xl font-bold text-slate-900">Acesse sua conta</h2><p className="mt-2 text-sm text-slate-500">Use suas credenciais corporativas para continuar.</p>{error&&<div className="mt-5 rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}<label className="mt-7 block text-sm font-semibold text-slate-700">E-mail<input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="mt-1.5 h-11 w-full rounded-lg border border-slate-300 px-3 outline-none ring-blue-500 focus:ring-2" placeholder="voce@empresa.com.br"/></label><label className="mt-4 block text-sm font-semibold text-slate-700">Senha<div className="relative mt-1.5"><input value={password} onChange={e=>setPassword(e.target.value)} type={show?"text":"password"} className="h-11 w-full rounded-lg border border-slate-300 px-3 pr-10 outline-none ring-blue-500 focus:ring-2"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-3 text-slate-400">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></label><Button disabled={loading} className="mt-7 h-11 w-full">{loading?"Validando acesso...":"Entrar"}<LockKeyhole size={16}/></Button><p className="mt-6 text-center text-xs text-slate-400">Ambiente protegido · Concilia ERP</p></form></section></main>}
